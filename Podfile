# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SafeAndSound' do

  use_frameworks!

  # Pods for SafeAndSound
  pod 'GoogleMaps'
  pod 'GooglePlaces'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'

end

target 'SafeAndSound WatchKit App' do
  
  use_frameworks!

  # Pods for SafeAndSound WatchKit App

end

target 'SafeAndSound WatchKit Extension' do
  
  use_frameworks!

  # Pods for SafeAndSound WatchKit Extension

end
